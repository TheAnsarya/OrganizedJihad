
****************************
Step One: Install Tamper Monkey https://www.tampermonkey.net/
Step Two: Install the HWH Script https://greasyfork.org/en/scripts/450693-herowarshelper
****************************



#new A new project named `OrganizedJihad` that is a javascript module for browsers using TamperMonkey that will log and track information from the online `Hero Wars` game @ https://www.hero-wars.com and then also have a browser ui that displays the information and reports and allows for managementang long term and short terms goals, with calendar and suggestions, etc. This should exist in the standard `source/repos` baqse-folder and be created as a new project on github.

&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&
directives:
for all:
use my editorconfig for formatting, use crlf, use tabs for all documents, use UTF-8, and always use the most modern code and practices, leave blank line between the stages, keep opening braces on the previous line, use yarn instead of npm when appropriate, comment everything including the code inside methods, add links in the comments to explanitory articles, always format the code using editorconfig and other formatting directives (never spaces, always tabs), if a file grows too large move pieces into their own logical components or classes separating concerns, update the chat log files, create a git commit including description, for this session and all sessions
for `c#/dotnet`:
use file-scoped namespace with a single blank line after, use [] instead of array.empty, use collection expressions, leave blank line between the stages, keep opening braces on the previous line, always use span when you can and it is right to do so
&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

----------------
Next Steps:
Build the script: yarn build (already works!)
Install in TamperMonkey: Open organized-jihad.user.js in browser
Test on Hero Wars: Visit https://www.hero-wars.com
Push to GitHub: Create repo and push
----------------------

save all of the past and future copilot chats to a folder so I know what's happening
^^^^^^^^^^^^^^^^^^^^^^^^^^^
Check `~docs/copilot-chats/` for past decisions, Review `.github/copilot-instructions.md` for coding standards, save all of the past and future copilot chats to a folder so I know what's happening
^^^^^^^^^^^^^^^^^^^^^^^^^^^
Push to GitHub: Create repo and push
The `OrganizedJihad` repos already exists, use that
install `GitHub CLI` for future use
install prettier and use the editorconfig for styling and use crlf, tabs (4), and utf-8, etc
use `https://hw-assist.com/` as an example of how to use and hook into the HeroWars API and also `https://greasyfork.org/en/scripts/450693-herowarshelper`
Add `C:\Users\me\source\repos\OrganizedJihad\~reference-code` to gitignore, and then for the `hw-assist` reference code, use `C:\Users\me\source\repos\OrganizedJihad\~reference-code` and expand the api and ui
format everything using prettier
No, there are tons of lines of code using spaces instead of tabs, fix those

******************
Get-ChildItem -Path src -Recurse -Include *.js,*.css | ForEach-Object { $content = Get-Content $_.FullName -Raw; $content = $content -replace '(?m)^( {4})+', { param($match) "`t" * ($match.Value.Length / 4) }; Set-Content -Path $_.FullName -Value $content -NoNewline }
*****************

no, you removed the indentation instead of using tabs, revert and fix it

******************
$files = Get-ChildItem -Path src -Recurse -Include *.js,*.css; foreach ($file in $files) { $content = Get-Content $file.FullName -Raw; $lines = $content -split "`r`n"; $newLines = @(); foreach ($line in $lines) { if ($line -match '^( +)') { $spaces = $matches[1].Length; $tabs = [math]::Floor($spaces / 4); $remainder = $spaces % 4; $line = ("`t" * $tabs) + (' ' * $remainder) + $line.Substring($spaces); }; $newLines += $line; }; $newContent = $newLines -join "`r`n"; Set-Content -Path $file.FullName -Value $newContent -NoNewline }
*************************

Move this project, not `~docs`, into a subfolder so we can add other related projects to the workspace, also make sure to keep the chat history up to date

Use the API research to go ahead and hook into all the api calls to HW (`Hero Wars`) and track all of that data. What is the current state, what was the historical state, what are the chances of getting items from each chest, etc, what users are you winning against, losing against, with what teams, etc.

******************************
Next Steps
Install the userscript and it will automatically:

Intercept all Hero Wars API calls
Build historical database as you play
Calculate drop rates from chest openings
Track W/L against every opponent
Record team compositions and battle results
Create trend analysis over time
***********************************
where is the tracking and other data stored? We want to store them long time, do we need a C#dotnet10 back project running at a publicly available URL for long term and collective storage? And a local SQL db to store everything for shorter term storage that we can then upload to the remote c# API.
&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&
Next steps:
Desktop App (.NET MAUI or Electron) with SQLite
C# .NET 10 Web API with full cloud infrastructure
Update userscript with sync capabilities
All three for complete architecture
&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&
make a fully complete TODO list for Tier 1 to 3 and also the `Next Steps` listed above, implementing all of the reccomendations and missing parts, and everything. Make sure to plan everything. After that, start implementing todo lists
sure!     yes!
gitignore bin and obj and the rest of them
continue
save all of the past and future copilot chats to a folder so I know what's happening

the API should be separate from the desktop app, webapi project, ignore the desktop app for now, make a database and web api to recieve data updates and retrieve them for the userscript. but keep a local version in the userscript -- can you use sqlite from an extension?

**********************
Would you like me to:
A) Quickly fix these remaining compile errors
B) Create a simplified working version without all the helper methods
C) Document what you need to fix and move on
***********************
A
************************
Next Steps:
1. Update the browser userscript to use IndexedDB for local storage (not SQLite)
2. Add HTTP client to POST data to the API /api/sync/import endpoint
3. Test the full sync workflow
4. Build the desktop UI pages
************************
Do 1 and then 2

---- git branch api-backend-creation
---- git reset --hard HEAD~4
---- git branch api-backend-creation-with-desktop
---- then back to the not `with-desktop` branch, delete the desktop app folder (we can merge it back in later, but no errors in build for now)

Make a big TODO list for moving the DB layer from the `api` project to its own project, add audit fields, with audit interfaces/abracts. especially `DateCreated` since most of these records will be unwritable in the data recording tables. explore the api to make sure we are recording everything useful in the database.

&&&& old directives

find the next TODO ensuring proper build order and implement it

&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&
directives:
for all:
use my editorconfig for formatting, use crlf, use tabs for all documents, use UTF-8, (IMPORTANT) and always use the most modern code and practices, leave blank line between the stages, keep opening braces on the previous line, use yarn instead of npm when appropriate, comment everything including the code inside methods, add links in the comments to explanitory articles, always format the code using editorconfig and other formatting directives (never spaces, always tabs), if a file grows too large move pieces into their own logical components or classes separating concerns, update the chat log files, create a git commit including description, for this session and all sessions
for `c#/dotnet`:
use the most recent version of dotnet and all other libraries, use the most modern development and styling practices, use file-scoped namespace with a single blank line after, use [] instead of array.empty, use collection expressions, leave blank line between the stages, keep opening braces on the previous line, always use span when you can and it is right to do so
&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&

dotnet format

update the chat log files, find the next TODO ensuring proper build order and implement it, create a git commit including description, and then update the chat log files
if a file grows too large move pieces/refactor into their own logical components or classes separating concerns
find the next TODO for the data layer, ensuring proper build order and implement it, make sure to follow the formatting, modern practices and other rules, and then update the chat log files, create a git commit including description
continue with `Phase 2: Add audit infrastructure`
update the chat log files, and then continue with `Phase 2.4: Implement EF Core SaveChanges interceptors`

****************************
Next Step: The TODO file shows Phase 3 (Update Database Context) but we've essentially completed those tasks using the interceptor pattern. We can either:
1. Continue with Phase 4 (Review API Data Collection)
2. Create an EF Core migration to add audit columns to the database schema
3. Review and update the TODO file to reflect the interceptor implementation
What would you like to do next?
*****************************
3, then 2, then 1

*************************
Would you like to continue with Phase 4 (Update API and Services), or would you prefer to work on something else?
****************************
continue with Phase 4 (Update API and Services)
Phase 5: Update Desktop App 
bring back the desktop app code from previous git commits, commit to current branch, and then proceed with phase 5
proceed with Phase 6 now
make sure the chat log files are up to date
continue with `Phase 3: Review API Data Collection` and definitely track everything about pets and titans and heros and inventory and everything else we can track
Phase 7: Update Userscript

create a comprehensive todo for testing the api, database, and userscript, and then begin implementing testing in stages. Remember: modern practices, comment everything, include links to external resources, update the chat log as you go.

do 2, 3, and 4 in order. Remember: modern practices, comment everything, include links to external resources, update the chat log as you go.
Activity Tracking Completion - Add more resource/guild tracking hooks

---- use VS and chat-cpt
 uninstall the powershell extension `ohmyposh` and return the powershell terminal to normal text colors because currently the text is showing as black on black and is unreadable
do it
----
fix the build errors and warnings, etc, for all the projects, especially `desktopapp` keeping the aplications platform agnostic











log guild chat and private chats and adventure chats and AoC chats












Setup testing for the userscript, database, and api
























































Check `~docs/copilot-chats/` for past decisions, Review `.github/copilot-instructions.md` for coding standards









