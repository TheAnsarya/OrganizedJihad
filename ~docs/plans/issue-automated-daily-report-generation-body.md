## Summary
Start remaining roadmap item #17 (Automated daily report generation) by adding scheduled report generation, persistence, and CSV export endpoints.

## Proposed slice
- Add a server-side daily report generator service
- Add schedule trigger/runner path for local runtime
- Add report persistence metadata and latest-report retrieval endpoint
- Add CSV export endpoint and UI links
- Add integration tests and benchmark coverage for report generation hot paths
